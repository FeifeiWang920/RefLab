# RefLab 可复现构建锁定（2026-09-03 于 Windows 11 / Python 3.13.5 验证）
# 用法：pip install -r requirements.txt -c constraints.txt
# 或：  pip install -e ".[dev,step,catia]" -c constraints.txt
numpy==2.5.2
numba==0.67.0
sv-ttk==2.6.1
cadquery==2.8.0
pywin32==312
pytest==9.1.1
ruff==0.16.5
